LOCALPATH=$HOME/opt/local
export PATH=$LOCALPATH/bin:$PATH

. $HOME/.bash_homebrew

. $HOME/.bash_cpp

. $HOME/.bash_ruby

. $HOME/.bash_python

. $HOME/.bash_node

. $HOME/.bash_term

. $HOME/.bash_opt
